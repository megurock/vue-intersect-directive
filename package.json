{
  "name": "vue-intersect-directive",
  "version": "0.1.2",
  "description": "Vue directive to observe intersection of an element with viewport.",
  "main": "dist/vue-intersect-directive.js",
  "module": "dist/vue-intersect-directive.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/megurock/vue-intersect-directive.git"
  },
  "scripts": {
    "start": "npm run dev",
    "dev": "rollup -w -c -m inline",
    "build": "rollup -c --sourcemapExcludeSources --compact",
    "prepublish": "npm run build",
    "lint": "tslint --project . --fix"
  },
  "keywords": [
    "vue",
    "plugins",
    "intersection-observer"
  ],
  "author": "megurock",
  "license": "MIT",
  "devDependencies": {
    "@types/vue": "^2.0.0",
    "rollup": "^1.19.4",
    "rollup-plugin-typescript": "^1.0.1",
    "tslint": "^5.18.0",
    "typescript": "^3.5.3"
  }
}
